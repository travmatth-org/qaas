version: 0.1

phases:
  install:
    commands:
      - make get
  pre_build:
    commands:
      - make cicd
  build:
    commands:
      - make build

artifacts:
  files:
    - dist/httpd
    - web/*
    - build/ci/appspec.yml
    - scripts/codedeploy/*
    - init/httpd.service