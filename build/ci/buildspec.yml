version: 0.1

phases:
  install:
    commands:
      - make get
  pre_build:
    commands:
      - make cicd
  build:
    commands:
      - make build.all

artifacts:
  files:
    - dist/*
  discard-paths: yes