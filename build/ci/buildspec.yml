version: 0.1

phases:
  install:
    commands:
      - make get
  pre_build:
    commands:
      - make check
  build:
    commands:
      - make build

artifacts:
  files:
    - dist/codedeploy
    - web/
    - build/ci/appspec.yml