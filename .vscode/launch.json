{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Server",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/faas/main.go",
            "args": ["--dev", "--port", "8080", "--static", "${workspaceFolder}/web"]
        }
    ]
}